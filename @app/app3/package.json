{
  "name": "@app/app3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 6003",
    "build": "vue-tsc && vite build",
    "serve": "vite preview --port 5003 --strictPort",
    "buildwatch": "vue-tsc && vite build --watch && vite preview --port 5003 --strictPort",
    "preview": "vite preview --port 5003 --strictPort",
    "clean": "rm -rf dist",
    "stop": "kill-port --port 5003"
  },
  "dependencies": {
    "vue": "^3.2.47"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.1.0",
    "typescript": "^4.9.3",
    "vite": "^4.2.0",
    "vue-tsc": "^1.2.0"
  }
}
